12 apr:-
    img tag 
    object tag
    unordered list --ul tag
    ordered list --ol tag with type tag
    description list dl tag with dt tag
    blockquite tag to  write about something from other sites
    q tag for quotation
    table tag
13 apr:-
    form
14 apr:-
    class startr with dot in style in head
    padding attribute to get the space bw text and border
    id start with # in style in head